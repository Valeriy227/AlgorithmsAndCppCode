// #include <bits/stdc++.h>

// using namespace std;

// typedef long long ll;

// int d;
// vector<vector<ll>> v;
// vector<pair<int, int>> segm;

// void get_dp(int l, int r, vector<vector<ll>>& dp) {
//    int n = r - l;
//    dp.resize(n, vector<ll>(d));
//    for (int i = 0; i < n; ++i) {

//    }
// }

// void divide(int l, int r, vector<int> ind) {
//    int mid = (l + r) / 2;

//    for (int i = 0; i < )
// }

// int main() {
//    int n;
//    cin >> n >> d;
//    v.resize(n);
//    for (auto& i : v) {
//        cin >> i;
//    }

//    int m;
//    cin >> m;
//    segm.resize(m);
//    for (auto& i : segm) {
//        cin >> i.first >> i.second;
//    }
// }
